# 🚀 Деплой Mini Battle Royale на Render.com (бесплатно, 24/7)

## Что получишь
- ✅ Сервер работает 24/7 в дата-центре Frankfurt (низкий пинг для EU)
- ✅ Свой URL вида `https://battleroyalemini.onrender.com`
- ✅ Друзья заходят всегда — твой ПК выключен
- ⚠ Бесплатный план "засыпает" через 15 мин без активности, просыпается за ~30 сек

---

## Шаг 1 — GitHub (3 минуты)

### 1.1. Создай аккаунт (если нет)
👉 https://github.com/signup
(можно через Google)

### 1.2. Создай новый репозиторий
👉 https://github.com/new
- **Repository name:** `battleroyalemini` (или любое другое)
- **Public** (на бесплатном Render только публичные репо)
- НЕ ставь галочки "Add README" / "Add .gitignore" — у нас уже всё есть
- Жми **Create repository**

### 1.3. Залей файлы (БЕЗ установки git, прямо в браузере)
На странице нового репо увидишь ссылку:
**"uploading an existing file"** (или жми **"Add file" → "Upload files"**)

Перетащи в окно браузера **ВСЕ файлы** из папки `Desktop\BattleRoyale\` **КРОМЕ:**
- ❌ `node_modules/` (если есть)
- ❌ `cloudflared.exe`
- ❌ `*.log` / `*.err`
- ❌ `package-lock.json`

**Файлы которые НУЖНО загрузить:**
- ✅ `server.js`
- ✅ `game.js`
- ✅ `sounds.js`
- ✅ `index.html`
- ✅ `package.json`
- ✅ `render.yaml`
- ✅ `Procfile`
- ✅ `.gitignore`
- ✅ `README.md`

Внизу страницы → "Commit changes" → жми зелёную кнопку.

---

## Шаг 2 — Render.com (2 минуты)

### 2.1. Регистрация
👉 https://render.com/signup
Выбери **"Sign up with GitHub"** (так удобнее).

### 2.2. Подключение репо
1. На дашборде жми **"New +"** → **"Web Service"**
2. **Connect a repository** → найди свой `battleroyalemini` → жми **Connect**
3. Render автоматически прочтёт `render.yaml` — все настройки уже заданы
4. Если попросит подтвердить — оставляй всё как есть, нажми **"Create Web Service"**

### 2.3. Жди ~3 минуты
Render будет:
- 📦 Скачивать твой код
- 🛠 Запускать `npm install`
- 🚀 Запускать `node server.js`

Когда увидишь "Live" вверху страницы — готово!

### 2.4. Твоя ссылка
Сверху страницы будет URL вида:
```
https://battleroyalemini.onrender.com
```

**Это твой постоянный URL.** Делись с друзьями. Работает 24/7.

---

## Шаг 3 — Играем!

Открой ссылку → **🌐 MULTIPLAYER** → ник → **ИГРАТЬ**.

Поле сервера автоматически заполнится `wss://battleroyalemini.onrender.com`.

---

## ❓ FAQ

**Q: Сервер тормозит первые 30 секунд при заходе.**
A: Это "пробуждение" с холодного старта на free плане. Один раз в 15 минут. Чтобы не засыпал — можно использовать сервис типа UptimeRobot который пингует URL каждые 5 минут (бесплатно).

**Q: Изменил код, как обновить?**
A: На GitHub → "Add file" → "Upload files" → перетащи изменённые файлы → Commit. Render автоматически перезапустит сервер с новой версией (autoDeploy: true в `render.yaml`).

**Q: Бесплатный план кончился?**
A: 750 часов/мес = 31 день. На один сервис хватит впритык. Если истечёт — Render предложит апгрейд за $7/мес или просто остановит сервис до следующего месяца.

**Q: Хочу выбрать регион ближе ко мне.**
A: В `render.yaml` поменяй `region: frankfurt` на:
- `oregon` (Запад США)
- `ohio` (Восток США)
- `singapore` (Азия)
- `frankfurt` (Европа) ← рекомендую для нас

---

🎮 **Удачи!**
